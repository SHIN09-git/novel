# TODO

本文件用于真实试用版之后的迭代排期。优先级含义：

- P0：数据安全 / 破坏性 bug，影响用户作品安全或可能造成误写、丢失、污染长期记忆。
- P1：生成质量 / 连贯性，影响章节可读性、上下文控制和 AI 输出可信度。
- P2：UI 体验，影响长期写作舒适度和操作效率。
- P3：未来功能，值得做但不阻塞真实试用。

## P0 数据安全 / 破坏性 bug

- 为 API Key 增加系统安全存储或本地加密存储，不再只随 JSON 数据保存。
- 为数据路径迁移增加“合并已有数据文件”的安全交互；当前只支持覆盖或取消。
- 为生产流水线增加更细的并发锁和“从某一步继续运行”，避免重复点击启动多个任务。
- 继续补充草稿、修订版本、章节正文写回的回归测试，重点覆盖 draft-only、关联章节丢失、重复接受候选等边界。
- 为 AI JSON 返回增加更严格的 schema 校验和字段级错误提示，避免异常结构进入候选区。
- 将 `MemoryUpdateCandidate.proposedPatch` 从 JSON 字符串升级为结构化 schema，降低应用候选时的解析风险。
- 完善 IPC 错误分类和用户提示，确保文件保存、数据迁移、剪贴板失败都不会造成静默失败。

## P1 生成质量 / 连贯性

- 为 Prompt 自动压缩实现真实内容裁剪和摘要替换，而不仅是选择/省略上下文。
- 为 ContextBudgetManager 增加更精确的“当前章节相关性”评分，例如读取章节任务书、角色出场计划和伏笔预计回收范围。
- 为伏笔 treatmentMode 增加历史趋势和节奏提示，避免同一高权重伏笔连续多章被推进。
- 为质量门禁增加可配置阈值、历史趋势和人工复核记录。
- 将 QualityGateReport 和 ConsistencyReviewReport 的 issue schema 进一步统一，减少重复展示。
- 为章节连续性桥接增加自动提取置信度和前后章节衔接链路可视化。
- 将 RedundancyReport 升级为段落级报告，支持点击建议直接进入修订工作台对应段落。
- 为冗余描写检测增加跨章节重复环境 / 机制说明识别；当前主要是单章内轻量规则检测。
- 为修订候选增加局部 diff 预览和一键重新跑质量门禁。
- 为 Revision Studio 增加真实选区捕获、段落级 diff、版本命名、修订链路过滤和接受后自动重跑质量门禁。

## P2 UI 体验

- 为复制、导出、保存、应用候选等操作增加统一 Toast 系统，替代部分页面内提示和 alert。
- 继续统一 ConfirmDialog，覆盖删除、覆盖、恢复旧版本、接受低质量草稿等危险操作。
- 为章节正文编辑器增加更强的长文本体验：专注模式、快捷键、搜索、字数目标提示和段落导航。
- 为 Dashboard、Prompt 构建器、生产流水线增加截图级布局回归测试，防止后续 UI 退化。
- 为右侧上下文面板增加页面级摘要，例如当前项目 token 状态、高权重伏笔、近期角色变化。
- 为 Prompt 构建器的上下文快照增加更清晰的详情弹窗和差异比较。
- 为 Generation Run Trace 增加更友好的摘要视图，减少查看 JSON 的需求。
- 为 ChapterVersion 增加按来源 / 时间筛选、恢复前差异预览和历史版本占用空间提示。

## P3 未来功能

- 接入 SQLite Repository 实现，替换当前 JSON Storage。
- 增加 AI 调用取消、重试队列和请求日志面板，但日志必须脱敏 API Key 和正文。
- 为 GeneratedChapterDraft 增加正文差异对比与“另存为新章节编号”的完整交互。
- 为时间线增加可视化轴视图。
- 为章节导出增加按卷 / 分组导出、文件编码选择和导出模板配置。
- 增加单元测试与端到端 UI 冒烟测试体系。
- 继续拆分 `App.tsx` 和页面 hooks，降低单文件维护成本。
- 将 `AIService` 中的字段归一化函数继续拆到 schema validator，逐步减少手写 `unknown` 处理分散在服务内部。
- 为角色关系、伏笔网络和世界观规则增加图谱视图。
- 为长篇项目增加卷、幕、篇章结构管理。
